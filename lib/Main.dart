import 'package:flutter/material.dart';
import 'MyApp.dart';

/// This function initializes and runs the app by calling the `runApp` method
/// with `MyApp` as the root widget.
void main() {
  runApp(MyApp() as Widget);
}
