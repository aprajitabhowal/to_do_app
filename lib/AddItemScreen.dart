import 'package:flutter/material.dart';
import 'AddItemScreenState.dart';

/// The `AddItemScreen` is a stateful widget that provides the logic necessary
/// to input and add new items to a shopping cart.
/// It delegates state management to the `AddItemScreenState` class.
class AddItemScreen extends StatefulWidget {
  @override
  AddItemScreenState createState() => AddItemScreenState();
}
